{% set name = "coredns" %}
{% set go_pkg_name = "github.com/coredns/" + name %}
{% set version = "1.8.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/coredns/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: cd5dee37f82b331862483df8029d6c7c33dbd29581de2876df93348055e28ab7

build:
  number: 0
  script:
    - go install -v -ldflags "-s -w" .

requirements:
  build:
    - {{ compiler('go') }} 1.13

test:
  commands:
    - coredns -version

about:
  home: https://coredns.io/
  license: Apache-2.0
  license_file: LICENSE
  license_family: Apache
  summary: CoreDNS is a DNS server written in Go that chains plugins
  doc_url: https://coredns.io/manual/toc/
  dev_url: https://github.com/coredns/coredns

extra:
  recipe-maintainers:
    - sodre
